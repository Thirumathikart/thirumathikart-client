import 'package:flutter/material.dart';

class Category {
  final String? image, title, route;
  final int? id;
  final Color? color;
  Category({
    this.id,
    this.image,
    this.title,
    this.color,
    this.route,
  });
}
